# DriftClip 第一版完整方案

## 1. 目标与边界

DriftClip 是一个自托管的多平台文本粘贴板历史服务。用户在各设备上复制文本或手动输入文本后，客户端将内容上传到服务端；同一账户下的设备和 Web 端都可以查看、复制及管理这些历史记录。

第一版支持 Windows、macOS、Linux、Android、iOS 和 Web。

### 1.1 第一版目标

- 采集和保存纯文本粘贴板内容。
- 支持手动输入文本并上传。
- 同一账户下的所有原生客户端与 Web 均可查看远端历史。
- 支持删除历史、筛选历史、复制某条历史到本机剪贴板。
- 支持多用户，数据按账户严格隔离。
- 使用 Web 账户登录管理 Key；原生客户端使用 Key 访问账户历史。

### 1.2 明确不做

- 不自动把远端记录写入其他设备的系统剪贴板。
- 不承诺 iOS 或 Android 的后台持续粘贴板监听。
- 不支持富文本、图片、文件或其他非文本剪贴板类型。
- 不做邮箱验证、密码找回、账户删除。
- 不做多 Key、Key 自定义、Key 标签或共享授权。
- 不做服务端内容去重、上传频率限制、注册频率限制。
- 不做应用层静态加密和自动数据库备份。
- 不做全文索引、分词、正则搜索、拼写纠错、WebSocket 或推送。

## 2. 产品模型

### 2.1 账户与设备

- 一个 Web 账户由规范化后的邮箱和密码标识。
- 邮箱注册时去除首尾空格、转小写，并且全局唯一；仅做基础格式检查（包含 `@`），不验证邮箱所有权。
- 密码只要求非空，不设长度、复杂度或强度规则。
- 每个账户只有一个有效 Key。
- 同一账户的全部原生客户端使用同一个 Key，访问同一组历史数据。
- 每次原生客户端安装时生成一个随机 UUID 作为安装 ID，安全保存；它不来自硬件，重装后会变化。

### 2.2 Key 生命周期

1. 用户完成注册后可登录 Web，但账户初始没有 Key。
2. 用户在 Key 管理页主动生成初始 Key。
3. Key 由服务端生成，用户不能自定义；建议格式为 `dc_` 前缀加至少 32 字节随机值。
4. 完整 Key 只在创建或重置完成时展示一次；服务端只保存 Key 哈希。
5. 每次原生 API 请求均通过 `Authorization: Bearer <Key>` 请求头携带完整 Key。
6. 重置 Key 的按钮在 5 秒倒计时结束前不可点击。
7. 重置完成后，新 Key 立即生效，旧 Key 立即且永久失效，历史数据保留。
8. Key 可读取、上传和删除所属账户的全部历史，但不能登录 Web、管理 Key 或修改密码。

### 2.3 Web 会话

- Web 使用邮箱和密码登录，不使用 Key 登录。
- 登录成功后建立 30 天有效的安全 Cookie 会话。
- 提供当前浏览器的退出登录功能。
- 提供修改密码功能，必须输入当前密码；修改成功后所有 Web 会话立即失效。
- Cookie 使用 `Secure`、`HttpOnly`、适当的 `SameSite` 属性；所有状态修改接口使用 CSRF 防护。
- 密码使用 Argon2id 哈希保存，绝不保存明文。

## 3. 粘贴板记录

### 3.1 记录字段

每条记录归属一个账户，并至少包含：

| 字段 | 说明 |
| --- | --- |
| `id` | 服务端生成的记录 ID |
| `account_id` | 账户归属 |
| `content` | 原始纯文本内容 |
| `source` | `clipboard` 或 `manual` |
| `received_at` | 服务端接收时间，唯一排序和淘汰依据 |
| `public_ip` | 服务端从请求来源观测到的公网 IP |
| `platform` | `windows`、`macos`、`linux`、`android` 或 `ios` |
| `os_version` | 客户端上报的系统版本 |
| `device_model` | 客户端可获取的设备型号或机型名称 |
| `app_version` | 客户端版本 |
| `installation_id` | 客户端生成的随机安装 UUID |

设备元数据仅用于展示和筛选，不作为安全依据。不得采集硬件序列号、MAC 地址、广告 ID、安装应用列表或精确位置。

### 3.2 文本规则

- 第一版只接收 UTF-8 纯文本。
- 非空文本按原样保存，不改变空格、换行或 Unicode 字符。
- 空剪贴板不上传。
- 手动输入去除首尾空格后为空时不允许提交。
- 单条正文上限由服务端配置 `max_clipboard_text_bytes` 控制，默认 `102400`，即 100 KiB。
- 超过上限时服务端拒绝请求并返回明确错误，不静默截断。
- Web 和原生端均以纯文本方式显示正文，不解析 HTML 或脚本。

### 3.3 时间规则

- `received_at` 由服务端在接收请求时写入，是排序、筛选和淘汰的唯一依据。
- 业务时区固定为 `Asia/Shanghai`，即 UTC+8；Web、客户端和业务日志均按该时区展示。
- 不依赖客户端时间，也不将客户端声明的复制时间用于保留策略。

### 3.4 保留与删除

- 记录上限按账户统一计算，不按设备单独计算。
- `max_history_records` 由服务端配置，默认 100。
- 写入新记录后，服务端按 `received_at` 保留最新的 N 条，删除最旧的超出记录。
- 删除记录时，正文、公网 IP 与全部设备元数据一并删除。
- 当服务重启且上限被调低时，启动阶段立即清理每个账户至新上限。
- 调高上限不会恢复已删除数据。

## 4. 上传与客户端去重

### 4.1 上传流程

1. 客户端监听到新文本，或用户在手动输入框提交文本。
2. 客户端校验文本非空和本地去重状态。
3. 客户端携带 Key、来源和设备元数据上传。
4. 首次请求立即发送。
5. 首次失败后等待 1 秒，发送第二次请求。
6. 第二次失败后等待 2 秒，发送第三次请求。
7. 任一次成功即结束重试，并更新本地去重状态。
8. 第三次失败后放弃该条内容；不建立内存外的离线队列，也不在以后自动补传。

Key 无效导致的 `401` 不属于网络失败：客户端立即停止上传和历史请求，显示需要更新 Key 的状态，不执行三次重试。用户填入新 Key 后恢复此前的监听开关偏好。

### 4.2 去重规则

- 去重只在客户端执行，服务端不做内容去重。
- 每个客户端维护自己最近一次**成功上传**的正文摘要。
- 新文本与最近成功上传的文本逐字节一致时，客户端不发起请求。
- 只有服务端成功响应后才更新摘要。上传失败、超时或网络中断均不更新，因此用户再次复制相同文本时仍会重新尝试上传。
- 去重摘要跨应用重启保存，保存内容为 SHA-256 摘要而非完整正文；更换 Key 时清除摘要。
- 去重状态只在单一客户端内生效。不同设备可能上传相同文本；服务端接受每个合法上传请求。
- 手动输入和系统剪贴板上传均遵守相同的去重规则。

## 5. 各平台客户端

### 5.1 技术栈与通用界面

- Windows、macOS、Linux、Android、iOS 共用 Flutter 原生客户端工程。
- 每个平台实现各自的剪贴板监听适配层，其他 UI 和 API 逻辑尽量共享。
- 每个原生客户端包含：Key 配置、监听开关、手动输入框、远端历史列表、详情、复制、筛选、删除、手动刷新和设置页。
- 原生端历史列表不自动轮询远端数据；用户可手动刷新。本设备成功上传后可更新当前列表。
- 远端记录详情提供复制按钮。复制到本机后若被监听器捕获，按普通 `clipboard` 事件处理，可能产生一条新记录。
- 原生端可删除单条、批量删除多条，或清空全部历史；清空全部需要 5 秒倒计时确认。

### 5.2 监听权限和后台范围

- 所有原生客户端在用户配置 Key 后默认关闭监听，必须由用户显式打开。
- 手动输入和历史查看不要求监听开启。
- iOS 只在 App 前台时读取文本粘贴板，不承诺后台实时捕获。
- Android 同样只在 App 前台读取文本粘贴板；不做默认输入法，也不承诺后台持续监听。
- Windows、macOS、Linux 可在应用常驻时监听系统粘贴板。

### 5.3 桌面端行为

- Windows、macOS、Linux 提供“登录后自动启动”设置，默认关闭。
- 开启监听时关闭主窗口，应用最小化到系统托盘并继续监听。
- 托盘菜单提供显示主窗口与明确退出应用的操作。
- 未开启监听时关闭主窗口即退出。

## 6. Web 端

### 6.1 技术栈

- Web 使用 React 构建独立静态站点。
- React 产物由 Go 服务静态托管。

### 6.2 历史查看与操作

- 历史默认按 `received_at` 倒序展示。
- 列表展示元数据和正文前 500 个字符预览；点击后加载完整正文和完整 IP。
- 每条详情提供复制按钮，复制是用户显式的本机操作。
- 支持单条删除和多选删除，均立即执行。
- 支持清空全部历史，点击后等待 5 秒确认再执行。
- 支持按平台筛选、正文普通子串模糊查询与 UTC+8 时间范围筛选。
- 多个筛选条件按 AND 组合；文本匹配忽略英文字母大小写，不做分词、正则或跨字段搜索。

### 6.3 刷新

- 始终提供手动刷新按钮。
- 自动刷新间隔只保存于当前浏览器本地存储，不写入账户。
- 默认关闭；`0` 表示关闭，允许的最短自动刷新间隔为 5 秒。
- 不使用 WebSocket、推送或服务端定时刷新。

## 7. 服务端与 API

### 7.1 技术栈

- 服务端使用 Go 编写单体 HTTP API。
- 数据库使用 SQLite，启用 WAL 模式。
- 使用事务保证创建记录、按账户淘汰旧记录及删除操作的一致性。
- React 构建产物由同一 Go 服务托管。
- API 使用 JSON 和 REST 风格路由。

### 7.2 权限边界

| 调用方 | 身份凭据 | 允许操作 |
| --- | --- | --- |
| Web | 账号密码建立的安全 Cookie 会话 | 查看、筛选、复制、删除历史；生成/重置 Key；修改密码；退出登录 |
| 原生客户端 | `Authorization: Bearer <Key>` | 查看、筛选、复制、上传、删除历史 |

- 所有 API 仅允许 HTTPS。
- Key、密码、会话 Cookie 与正文不得写入应用日志、访问日志或错误上报数据。
- 必须使用参数化 SQL，避免正文模糊查询和筛选条件引入 SQL 注入。
- 身份校验失败、权限校验失败和正文超限返回明确但不泄漏敏感信息的错误。

### 7.3 公网 IP

- 每次上传使用服务端观测到的请求来源公网 IP，写入对应记录。
- 不接受客户端提交的 IP。
- 部署在反向代理后时，应用仅信任配置中列出的反向代理，并从其转发头读取客户端 IP；未配置可信代理时使用直接连接地址。

## 8. 配置与部署

### 8.1 部署方式

- 以 Docker Compose 交付 Go 服务和 SQLite 持久化卷。
- SQLite 数据放在持久化 Docker volume 中。
- HTTPS、域名、证书申请与公网反向代理由部署者负责；应用不直接暴露给公网。
- 客户端服务地址以打包时内置的配置文件提供，不写死在业务代码中。
- 不提供自动数据库备份。持久化卷、备份和灾难恢复由部署者自行负责。

### 8.2 建议配置项

```yaml
server:
  listen_addr: "0.0.0.0:8080"
  business_timezone: "Asia/Shanghai"
  trusted_proxies: []

registration:
  enabled: true

history:
  max_history_records: 100
  max_clipboard_text_bytes: 102400

security:
  session_duration_days: 30
  session_secret: "replace-with-a-long-random-secret"
  key_pepper: "replace-with-a-separate-long-random-secret"

database:
  path: "/data/driftclip.sqlite"
```

- 修改 `registration.enabled` 后重启服务生效。关闭注册只阻止新账户创建，不影响既有账户登录、Key 管理或数据访问。
- 修改 `history.max_history_records` 并重启后，服务启动时立即将每个账户清理到新上限。
- `session_secret` 与 `key_pepper` 只能由部署者安全保存，不能提交到代码库或打印到日志。

## 9. 关键风险与已接受取舍

| 取舍 | 结果 |
| --- | --- |
| 无邮箱验证和密码找回 | 用户忘记密码后无法恢复旧账户 |
| 仅一个共享 Key | Key 泄露可读取、写入和删除整个账户历史；重置 Key 是唯一止损方式 |
| 无上传或注册限流 | 开放注册和持有 Key 的客户端可能被滥用 |
| 无服务端去重 | 不同设备可产生相同正文的多条记录 |
| 无离线队列 | 连续三次失败后，该次内容永久不上传，除非用户再次复制或手动提交 |
| 无应用层静态加密 | 获得数据库文件或数据库访问权限的人可读取正文和元数据 |
| 无自动备份 | 数据库卷丢失、损坏或误删时无法由应用恢复 |
| 移动端仅前台监听 | iOS 和 Android 无法保证每次复制都被捕获 |

## 10. 验收标准

1. 两台配置同一 Key 的设备上传文本后，均能在原生端和 Web 查看同一账户的历史。
2. 不同账户的 Key 和 Web 会话绝不能读取或删除彼此的记录。
3. 每条记录能展示正文、来源、UTC+8 服务端时间、完整 IP 与约定的设备元数据。
4. 每个账户最多保留配置数量的最新记录，超出后按服务端接收时间删除最旧记录。
5. 同一客户端连续复制已经成功上传的相同文本时，不会再发请求；上传失败时不会更新去重状态。
6. Key 重置后旧 Key 立刻收到无效凭据响应，新 Key 可正常访问原有历史。
7. Web 和原生端均能单条删除、多选删除和在 5 秒确认后清空全部。
8. Web 支持平台、文本子串和时间范围组合筛选；原生端支持平台、文本子串和时间范围筛选。
9. iOS 和 Android 在前台可采集，桌面端在监听开启时可托盘常驻采集。
10. 明文 HTTP 请求、无效 Key、空正文和超限正文均被服务端拒绝。

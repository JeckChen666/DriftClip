## What does this PR do?

<!-- Brief description of the change and the motivation. -->

## Checklist

- [ ] `cd server && go vet ./... && go test ./...` passes (if server changed)
- [ ] `cd web && npm run lint && npm test` passes (if web changed)
- [ ] `cd client && flutter analyze && flutter test` passes (if client changed)
- [ ] UI changes follow the shared design tokens (`tokens/tokens.json`)
- [ ] New server behavior is covered by tests
